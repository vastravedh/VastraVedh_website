Place your VastraVedh logo image in this folder named exactly:

    logo.png

The header logo will pick it up automatically. Until the file exists,
the site falls back to a styled "VastraVedh" text mark.

Recommended: a transparent PNG, roughly 600x200px (3:1), so it scales
crisply in the header.
